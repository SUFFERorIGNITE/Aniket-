def computepay(h,r):
    if h > 40 :
        xp = h * r
        otp = (h - 40) * (r - 0.5)
        return otp + xp
    else :
        return h * r

hrs = input("Enter Hours:")
h = float(hrs)
rate = input ("Enter rate:")
r = float (rate)
p = computepay(h,r)
print("Pay",p)
